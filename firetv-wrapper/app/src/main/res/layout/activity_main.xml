<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/david_black">

  <WebView
    android:id="@+id/dashboard_webview"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:importantForAutofill="no"
    android:overScrollMode="never" />

  <LinearLayout
    android:id="@+id/fallback_screen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_fallback"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="48dp"
    android:visibility="gone">

    <TextView
      android:id="@+id/fallback_title"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:fontFamily="sans-serif-medium"
      android:text="@string/app_name"
      android:textAllCaps="true"
      android:textColor="@color/david_white"
      android:textSize="34sp" />

    <TextView
      android:id="@+id/fallback_message"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_marginTop="20dp"
      android:text="@string/reconnecting_message"
      android:textColor="@color/david_white"
      android:textSize="24sp" />

    <Button
      android:id="@+id/retry_button"
      android:layout_width="220dp"
      android:layout_height="72dp"
      android:layout_marginTop="28dp"
      android:backgroundTint="@color/david_blue"
      android:focusable="true"
      android:focusableInTouchMode="true"
      android:text="@string/retry"
      android:textAllCaps="true"
      android:textColor="@color/david_white"
      android:textSize="22sp" />
  </LinearLayout>

</FrameLayout>

